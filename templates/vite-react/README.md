# templates/vite-react

Canonical Workspace starter template.

New projects copy this tree into `data/projects/<id>/workspace/`.
Agent coding should primarily mutate `src/` under the workspace, not invent a new toolchain.
